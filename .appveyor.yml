version: '2.0.{build}'
dotnet_csproj:
  patch: true
  file: '**\*.csproj'
  version: '{version}'
  version_prefix: '{version}'
  version_suffix: 'beta'
  assembly_version: '{version}'
  file_version: '{version}'
  informational_version: '{version}'
image: Visual Studio 2019
build_script:
  - ps: dotnet restore .
  - ps: dotnet build . --no-restore --configuration Release
after_build:
  - ps: dotnet pack . --no-restore --no-build --configuration Release -o ./pack
artifacts:
  - path: '**\*.nupkg'